services:
  server:
    build:
      context: ./apps/server
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
      PORT: 2567
    expose:
      - "2567"
    networks:
      - rps-shared-net
    restart: unless-stopped

networks:
  rps-shared-net:
    external: true
